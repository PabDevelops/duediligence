export const metadata = {
  title: 'Search Stocks | Bulltrace',
  description: 'Search thousands of US stocks by ticker or company name.',
  robots: { index: false, follow: false },
};

export default function SearchLayout({ children }) {
  return children;
}
