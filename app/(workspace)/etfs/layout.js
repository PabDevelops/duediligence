export const metadata = {
  title: 'ETF Directory & Analysis | Bulltrace',
  description: 'Track and analyze exchange-traded funds (ETFs) including key holdings, sector weightings, AUM, and historical performance.',
  alternates: { canonical: 'https://bulltrace.app/etfs' },
};

export default function ETFsLayout({ children }) {
  return children;
}
